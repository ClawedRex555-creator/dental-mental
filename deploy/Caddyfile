{
  email {$ACME_EMAIL}
}

www.{$APP_ROOT_DOMAIN} {
  redir https://{$APP_ROOT_DOMAIN}{uri} permanent
}

{$APP_ROOT_DOMAIN} {
  encode gzip
  reverse_proxy app:3000
}

# Все клиники: elanar, tstom, … (сертификат Let's Encrypt на каждый поддомен при первом заходе)
*.{$APP_ROOT_DOMAIN} {
  encode gzip
  reverse_proxy app:3000
}
